<?xml version="1.0"?>
<!--
###################################################
##RDS Data Sink
###################################################
 -->
<block>
	<name>RDS Panel</name>
	<key>gr_rds_panel</key>
	<category>RDS</category>
	<import>from gnuradio import gr, rds</import>
	<import>import rdspanel</import><!-- FIXME -->
	<make>#set $parent = $notebook() and 'self.%s.GetPage(%s)'%$notebook() or 'self'
rdspanel.rdsPanel($(id)_msgq_in, $(parent).GetWin())
#if not $grid_pos()
$(parent).Add(self.$(id))
#else
$(parent).GridAdd(self.$(id), $(', '.join(map(str, $grid_pos()))))
#end if</make>
	<callback></callback>
	
	<!--<param>
		<name>Window Size</name>
		<key>win_size</key>
		<value></value>
		<type>int_vector</type>
		<hide>#if $win_size() then 'none' else 'part'#</hide>
	</param>-->

	<param>
		<name>Grid Position</name>
		<key>grid_pos</key>
		<value></value>
		<type>grid_pos</type>
	</param>

	<param>
		<name>Notebook</name>
		<key>notebook</key>
		<value></value>
		<type>notebook</type>
	</param>

	<!--<check>not $win_size or len($win_size) == 2</check>-->
	
	<sink>
		<name>in</name>
		<type>msg</type>
	</sink>
	<doc></doc>
</block>
